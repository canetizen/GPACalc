# GPA-Calculator
This repository includes a basic GPA calculator for university students with GUI. It supports only AA, BA, BB, CB, CC, DC, DD, FF, VF letter grades for now. Repository also includes its wrapped executable (.exe) version, yet it may causes antivirus warnings, and it requires Java Runtime Environment (JRE) installation.
